variable "resource_group_name" {
  type    = string
  default = "{{ resource_group_name }}"
}
variable "name" {
  type    = string
  default = "{{ name }}"
}
variable "tags" {
  type    = map(string)
  default = {}
}
