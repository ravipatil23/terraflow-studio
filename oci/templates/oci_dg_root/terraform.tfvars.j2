prefix         = "{{ prefix }}"
compartment_id = "{{ compartment_id or '<compartment-ocid>' }}"
primary_region = "{{ primary_oci_region }}"
dr_region      = "{{ standby_oci_region }}"

primary_vcn_id         = "{{ primary_vcn_id or '<primary-vcn-ocid>' }}"
primary_nsg_id         = "{{ primary_nsg_id or '<primary-nsg-ocid>' }}"
primary_client_cidr    = "{{ primary_client_cidr }}"
primary_route_table_id = "{{ primary_route_table_id or '<primary-default-rt-ocid>' }}"
primary_hub_cidr       = "{{ primary_hub_cidr }}"

dr_vcn_id         = "{{ standby_vcn_id or '<dr-vcn-ocid>' }}"
dr_nsg_id         = "{{ standby_nsg_id or '<dr-nsg-ocid>' }}"
dr_client_cidr    = "{{ standby_client_cidr }}"
dr_route_table_id = "{{ standby_route_table_id or '<dr-default-rt-ocid>' }}"
dr_hub_cidr       = "{{ standby_hub_cidr }}"
